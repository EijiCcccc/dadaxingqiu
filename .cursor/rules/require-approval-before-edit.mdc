---
description: 修改代码前必须获得用户同意，且仅在附加 flutter-code-rules 时允许修改
alwaysApply: true
---

# 修改代码前必须获得用户同意

**前置条件**：仅当用户已附加 `flutter-code` skill 时，才允许执行代码修改。未附加时，应拒绝修改并提示用户附加该 skill。

在**执行任何代码修改**（包括创建、编辑、删除文件）之前，必须先获得用户的明确同意。

## 工作流程

1. **先说明**：清楚描述你打算做什么修改、修改哪些文件、以及修改的原因
2. **等待确认**：在用户明确表示同意（如「可以」「同意」「执行」等）之后，再执行修改
3. **禁止擅自修改**：未经用户同意，不得使用 `StrReplace`、`Write`、`EditNotebook`、`Delete` 等会改变代码的工具

## 例外情况

- 用户明确要求「直接修改」「马上改」等指令时，可视为已同意
- 仅回答问题时，不涉及修改，无需确认
