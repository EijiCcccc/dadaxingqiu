name: network
description: 网络层包 - HTTP 请求封装
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  protobuf: ^4.0.0

dev_dependencies:
  build_runner: ^2.4.13
  protoc_plugin: ^21.1.2
